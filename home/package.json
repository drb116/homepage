{
  "name": "home",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sync:recipes": "node ../scripts/sync-recipe-assets.mjs",
    "predev": "npm run sync:recipes",
    "dev": "wrangler dev",
    "predeploy": "npm run sync:recipes",
    "deploy": "wrangler deploy",
    "test": "node --input-type=module --check < public/meals/meals.js && node --input-type=module --check < ../shared/recipes/recipe-collection.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
